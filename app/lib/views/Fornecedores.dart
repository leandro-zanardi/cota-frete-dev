import 'package:flutter/material.dart';

class Fornecedores extends StatelessWidget {
  const Fornecedores({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Fornecedores'));
  }
}
